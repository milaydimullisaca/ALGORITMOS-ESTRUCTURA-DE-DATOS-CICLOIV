x = 27
epsilon = 0.01
inf = 0
sup = x

m = (inf + sup) / 2.0

while abs(m**3 - x) >= epsilon:
    if m**3 < x:
        inf = m
    else:
        sup = m
    m = (inf + sup) / 2.0

print(m, "es aproximadamente la raíz cúbica de", x)
